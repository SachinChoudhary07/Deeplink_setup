name: deeplink_setup
description: Generate, validate, and diagnose Android App Links and iOS Universal Links.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  args: ^2.5.0
  crypto: ^3.0.6
  http: ^1.5.0
  path: ^1.9.0
  yaml: ^3.1.3
  xml: ^6.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0

executables:
  deeplink_setup: deeplink_setup
